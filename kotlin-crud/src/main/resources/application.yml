spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/kotlin?serverTimezone=UTC
    username: root
    password: kongtech

  jpa:
    hibernate:
      ddl-auto: create
    generate-ddl: true
    show-sql: true

server:
  port: 9091
